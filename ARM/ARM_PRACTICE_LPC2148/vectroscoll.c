#include"lcd.h"
#include"delay.h"
#include"lcd_defines.h"
main()
{
u8 i;
InitLCD();
{
for(i=0;i<16;i++)
{
CmdLCD(GOTO_LINE1_POS0+i);
StrLCD("VECTOR");
//delay_ms(2000);
CmdLCD(CLRSCR);
}
delay_ms(200);
for(i=15;i>0;i--)
{
CmdLCD(GOTO_LINE1_POS0+i);
StrLCD("VECTOR");
delay_ms(200);
CmdLCD(CLRSCR);
}}while(1);
}
